{
  "name": "ata-gov",
  "version": "1.0.0",
  "private": true,
  "description": "Grava reuniões presenciais/híbridas, transcreve com identificação de participantes e gera a ata seguindo a Redação Oficial da Administração Pública brasileira.",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "build": "npm --prefix web run build && npm --prefix server run build",
    "test": "npm --prefix server test",
    "start": "npm --prefix server start",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev"
  }
}
